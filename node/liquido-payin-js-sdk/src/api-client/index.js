const AuthClient = require('./AuthClient');
const BoletoClient = require('./BoletoClient');
const CreditCardClient = require('./CreditCardClient');
const PayInClient = require('./PayInClient');
const PixClient = require('./PixClient');

module.exports = {
    AuthClient,
    BoletoClient,
    CreditCardClient,
    PayInClient,
    PixClient
};